mysql --version      mysql 버전 확인

mysql 접속
mysql -u유저아디 -p 엔터
비밀번호 (눈에 보이지 않음)

mysql 나가기
quit;
exit;

show databases;  db의 리스트를 출력하라.
use 데이터베이스이름;   db를 사용한다.

show tables;

구조  desc 테이블이름;
목록 select host, user from user;

select *from 테이블명
select 컬럼명 or *from 테이블명 where 조건절 

CRUD
C - insert
R - select
U - update 
D - delete

C : insert into 테이블명 (컬럼명) values (컬럼내용);

R : select 컬럼명 from 테이블명 where 컬럼명 = '컬럼내용';

U : update 테이블 set 컬러명=컬럼내용 where 대상컬럼명=컬럼내용;

D : delete from 테이블명 where 컬럼='컬럼내용';

in 여러항목 한번에 가능
between 몇부터 몇까지

order by 컬럼 asc 오름차순 123 가나다 (기본값)
order by 컬럼 desc 내림차순 321 다나가

length() 문자열길이
count() 개수
sum() 
avg() 평균
max() 최대
min() 최소
upper() 대문자
lower() 소문자
round() 반올림
mid() 중간부분 리턴

join종류
inner join : 교집합 - 두테이블에서 모두 존재하는 데이터만 출력
left join : 조인시 join왼쪽테이블의 null값을 포함해서 표시
right join : 조인시 join오른쪽테이블의 null값을 포함해서 표시
full join : mysql은 지원하지않음

이름 길때 ex) countrycode as cc 

view
sql쿼리 결과값을 임시테이블로 만들어 사용하는 방법
사용용도가 끝나면 명시적으로 삭제해야한다 (Drop view...)
create view 이름 select...

일부데이터 로드 
mysql - limit 
sql server - top


DriverManger : JDBC Driver 클래스를 관리 db와 연결해서 connction 구현 객체를 생성한다.

connection

statement, preparedstatement callalbestatement


Dao - 데이터접속
Dto - 데이터 transfer

